commit_message: "fix(ui): Migliora il layout del Trainer e corregge il wrapping dei pulsanti nel modale"
rationale: |
  Questo commit implementa diverse correzioni mirate all'interfaccia utente per migliorare l'usabilità e la coerenza visiva, come delineato nel piano approvato.

  Le modifiche principali sono:
  1.  **Correzione Layout Modale Mobile**: In `styles/modals.css`, è stato aggiunto `flex-wrap: wrap` al contenitore dei pulsanti di azione del workout (`.workout-summary-actions`) nella media query per schermi piccoli. Questo risolve il problema di overflow, facendo sì che i pulsanti si impilino verticalmente in modo corretto.
  2.  **Riorganizzazione Strutturale Trainer**: Il template `templates/trainer.html` è stato modificato per spostare l'indicatore degli esercizi imminenti direttamente sotto i dettagli dell'esercizio corrente. Il contenitore a piè di pagina è stato rimosso per un layout più compatto.
  3.  **Ottimizzazione Spaziatura Trainer**: In `styles/trainer.css`, il padding superiore del corpo del trainer è stato ridotto e gli stili per il vecchio contenitore degli esercizi imminenti sono stati eliminati. Sono stati aggiunti nuovi stili per il nuovo elemento `#upcoming-exercise-display` per integrarlo visivamente.
  4.  **Aggiornamento Logica Componente Trainer**: Il file `src/ui/components/TrainerComponent.js` è stato aggiornato per riflettere le modifiche al DOM. Ora gestisce il nuovo elemento `#upcoming-exercise-display`, popolandolo con un elenco conciso dei prossimi esercizi, migliorando la presentazione delle informazioni.
patches:
  - file: styles/modals.css
    content: |
      /* --- MODALS --- */
      .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.7); display: flex; justify-content: center; align-items: center; z-index: 1001; opacity: 0; pointer-events: none; transition: opacity 0.3s ease; }
      .modal-hidden { display: none !important; }
      .modal-overlay:not(.modal-hidden) { display: flex; opacity: 1; pointer-events: all; }
      .modal-content { background-color: var(--card-color); border-radius: 8px; border: 1px solid var(--border-color); width: 90%; max-width: 600px; display: flex; flex-direction: column; max-height: 90vh; position: relative; }
      .modal-header { padding: 1rem 1.5rem; border-bottom: 1px solid var(--border-color); }
      .modal-header h3 { color: var(--primary-color); }
      .modal-close-btn { position: absolute; top: 0.5rem; right: 0.5rem; background: none; border: none; font-size: 1.5rem; cursor: pointer; color: var(--text-color-muted); padding: 0; line-height: 1; width: 2.25rem; height: 2.25rem; display: flex; align-items: center; justify-content: center; border-radius: 50%; transition: background-color 0.2s, color 0.2s; z-index: 10; }
      .modal-close-btn svg { width: 1em; height: 1em; }
      .modal-close-btn:hover { background-color: rgba(255, 255, 255, 0.1); color: var(--text-color); }
      .modal-body { overflow-y: auto; flex-grow: 1; padding: 1.5rem; }
      .modal-footer { padding: 1rem 1.5rem; border-top: 1px solid var(--border-color); display: flex; justify-content: flex-end; gap: 1rem; }
      .selection-list { display: flex; flex-direction: column; gap: 0.5rem; }
      .selection-item { padding: 0.75rem; border: 1px solid var(--border-color); border-radius: 8px; cursor: pointer; transition: background-color 0.2s, border-color 0.2s; }
      .selection-item:hover { background-color: var(--bg-color); border-color: var(--primary-color); }
      .day-editor-list { display: flex; flex-direction: column; gap: 0.5rem; }
      .day-editor-slot { display: flex; align-items: center; gap: 1rem; padding: 0.75rem; background: var(--bg-color); border-radius: 8px; }
      .day-editor-slot .meal-type-label { font-weight: bold; color: var(--text-color-muted); flex-shrink: 0; width: 150px; }
      .meal-details-container { flex-grow: 1; min-width: 0; }
      .workout-summary { display: flex; justify-content: space-between; align-items: center; width: 100%; gap: 1rem; }
      .workout-summary-actions { display: flex; gap: 0.5rem; }
      .meal-details { display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; border-bottom: 1px solid var(--border-color); }
      .meal-details:last-of-type { border-bottom: none; }
      .meal-details__name { overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; font-weight: 500;}
      .meal-actions { display: flex; align-items: center; gap: 0.25rem; flex-shrink: 0; margin-left: auto; }
      .meal-actions button { background: none; border: none; color: var(--text-color-muted); cursor: pointer; padding: 0.4rem; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
      .meal-actions button:hover { background-color: rgba(255,255,255,0.1); color: var(--text-color); }
      .meal-actions .btn-remove-exercise { color: var(--danger-color); }
      .exercise-info { display: flex; flex-direction: column; gap: 0.25rem; }
      .exercise-details-summary { font-size: 0.85rem; color: var(--text-color-muted); }
      .btn-add-meal, .btn-add-exercise { background: transparent; border: 1px dashed var(--border-color); color: var(--text-color-muted); padding: 0.5rem 1rem; border-radius: 8px; cursor: pointer; width: 100%; transition: all 0.2s; margin-top: 0.5rem; }
      .btn-add-meal:hover, .btn-add-exercise:hover { background: var(--card-color); border-color: var(--secondary-color); color: var(--secondary-color); }
      #exercise-editor-form { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
      #exercise-editor-form .form-group { display: flex; flex-direction: column; }
      #exercise-editor-form label { margin-bottom: 0.5rem; font-size: 0.9rem; color: var(--text-color-muted); }
      #exercise-editor-form input { width: 100%; padding: 0.75rem; border: 1px solid var(--border-color); border-radius: 8px; font-size: 1rem; background-color: var(--bg-color); color: var(--text-color); }
      #exercise-editor-form .tempo-group { grid-column: 1 / -1; display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem; }
      .drag-handle { cursor: grab; color: var(--text-color-muted); padding: 0.5rem; }
      .draggable-item { background-color: var(--bg-color); border-radius: 8px; }
      .sortable-ghost { opacity: 0.4; background: var(--primary-color); }

      @media (max-width: 480px) {
        .workout-summary {
          flex-direction: column;
          align-items: flex-start;
          gap: 0.75rem;
        }
        .workout-summary-actions {
          width: 100%;
          display: flex;
          flex-wrap: wrap;
        }
        .workout-summary-actions .btn {
          flex-grow: 1;
        }
      }
  - file: styles/trainer.css
    content: |
      /* --- TRAINER VIEW STYLES --- */
      #trainer-page {
          height: calc(100vh - 60px);
          display: flex;
          flex-direction: column;
          background-color: var(--bg-color);
          color: var(--text-color);
      }

      .trainer-header {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 1rem;
          background-color: var(--card-color);
          border-bottom: 1px solid var(--border-color);
          flex-shrink: 0;
      }

      .trainer-header h2 {
          font-size: 1.25rem;
          color: var(--primary-color);
      }

      .trainer-body {
          flex-grow: 1;
          display: flex;
          flex-direction: column;
          justify-content: flex-start;
          align-items: center;
          padding: 1rem;
          text-align: center;
          position: relative;
          overflow-y: auto;
      }

      .exercise-info-container {
          margin-bottom: 1rem;
      }

      #current-exercise-name {
          font-size: 2rem;
          font-weight: bold;
      }

      #current-exercise-details {
          font-size: 1.25rem;
          color: var(--text-color-muted);
          margin-bottom: 0.5rem;
      }

      #current-rep-display {
        font-size: 1rem;
        color: var(--secondary-color);
        font-weight: bold;
      }
      
      #upcoming-exercise-display {
        font-size: 0.9rem;
        color: var(--text-color-muted);
        margin-top: 0.75rem;
        font-style: italic;
      }

      #upcoming-exercise-display strong {
        color: var(--text-color);
        font-style: normal;
      }

      .timer-ring-container {
          width: 280px;
          height: 280px;
          position: relative;
          display: flex;
          justify-content: center;
          align-items: center;
          margin: 1rem 0;
      }

      .timer-ring-display {
          font-size: 2.5rem;
          font-weight: bold;
          fill: var(--text-color);
          text-transform: uppercase;
          text-anchor: middle;
      }

      .timer-ring-bg {
          fill: none;
          stroke: var(--card-color);
          stroke-width: 12;
      }

      .timer-ring-progress {
          fill: none;
          stroke: var(--primary-color);
          stroke-width: 12;
          stroke-linecap: round;
          transform: rotate(-90deg);
          transform-origin: 50% 50%;
          transition: stroke-dashoffset 0.1s linear;
      }

      @keyframes flash {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
      }

      .flashing {
        animation: flash 1.4s infinite;
        fill: var(--secondary-color);
      }

      .trainer-controls {
          display: flex;
          gap: 1rem;
          justify-content: center;
          align-items: center;
          margin-top: 1rem;
          height: 80px;
      }

      .btn-large {
          padding: 1rem 2rem;
          font-size: 1.25rem;
      }
  - file: templates/trainer.html
    content: |
      <div id="trainer-page" class="page-view hidden">
          <header class="trainer-header">
              <button id="trainer-back-btn" class="btn btn-secondary">Indietro</button>
              <h2 id="trainer-title">Allenamento</h2>
              <button id="trainer-end-btn" class="btn btn-danger">Termina</button>
          </header>
          <div class="trainer-body">
              <div class="exercise-info-container">
                  <h3 id="current-exercise-name"></h3>
                  <p id="current-exercise-details"></p>
                  <p id="current-rep-display"></p>
                  <div id="upcoming-exercise-display"></div>
              </div>

              <div id="trainer-display-modes">
                  <div id="mode-tempo-guided">
                      <div id="timer-ring-container" class="timer-ring-container">
                          </div>
                  </div>
              </div>

              <div id="trainer-main-controls" class="trainer-controls">
                  <button id="trainer-start-btn" class="btn btn-primary btn-large hidden">AVVIA</button>
                  <button id="trainer-pause-btn" class="btn btn-secondary btn-large hidden">PAUSA</button>
                  <button id="trainer-resume-btn" class="btn btn-primary btn-large hidden">RIPRENDI</button>
              </div>
          </div>
      </div>
  - file: src/ui/components/TrainerComponent.js
    content: |
      import { startWorkout, pauseWorkout, resumeWorkout, endWorkout } from '../../core/trainer.js';
      import { setView } from '../../core/state.js';
      import { log } from '../../utils/logger.js';
      import { UI_TEXT } from '../../utils/constants.js';

      const RING_RADIUS = 100;
      const RING_CIRCUMFERENCE = 2 * Math.PI * RING_RADIUS;

      export class TrainerComponent {
          constructor(containerElement) {
              this.container = containerElement;

              this.elements = {
                  exerciseName: this.container.querySelector('#current-exercise-name'),
                  exerciseDetails: this.container.querySelector('#current-exercise-details'),
                  repDisplay: this.container.querySelector('#current-rep-display'),
                  upcomingDisplay: this.container.querySelector('#upcoming-exercise-display'),
                  
                  btnStart: this.container.querySelector('#trainer-start-btn'),
                  btnPause: this.container.querySelector('#trainer-pause-btn'),
                  btnResume: this.container.querySelector('#trainer-resume-btn'),

                  modeTempoContainer: this.container.querySelector('#mode-tempo-guided'),
                  ringContainer: this.container.querySelector('#timer-ring-container'),
              };

              this.ringProgress = null;
              this.ringText = null;
          }

          mount() {
              this.createTimerRing();
              this.container.addEventListener('click', this.handleControls.bind(this));
          }

          destroy() {
              log('TrainerComponent', 'Destroying component and cleaning up DOM...');
              this.container.removeEventListener('click', this.handleControls.bind(this));
              if (this.elements.ringContainer) {
                  this.elements.ringContainer.innerHTML = '';
              }
              log('TrainerComponent', 'Component destroyed.');
          }

          handleControls(e) {
              const targetId = e.target.id;
              log('TrainerComponent', `Control button clicked: ${targetId}`);
              switch (targetId) {
                  case 'trainer-start-btn': startWorkout(); break;
                  case 'trainer-pause-btn': pauseWorkout(); break;
                  case 'trainer-resume-btn': resumeWorkout(); break;
                  case 'trainer-end-btn': endWorkout(); break;
                  case 'trainer-back-btn': setView('planner'); break;
              }
          }

          createTimerRing() {
              if (this.elements.ringContainer.querySelector('svg')) {
                log('TrainerComponent', 'Timer ring SVG already exists. Skipping creation.');
                return;
              }
              log('TrainerComponent', 'Creating timer ring SVG...');
              this.elements.ringContainer.innerHTML = '';
              const svgNS = 'http://www.w3.org/2000/svg';
              const svg = document.createElementNS(svgNS, 'svg');
              const svgSize = (RING_RADIUS + 12) * 2;
              svg.setAttribute('width', svgSize);
              svg.setAttribute('height', svgSize);
              svg.setAttribute('viewBox', `0 0 ${svgSize} ${svgSize}`);

              const center = svgSize / 2;

              const backgroundCircle = document.createElementNS(svgNS, 'circle');
              backgroundCircle.setAttribute('cx', center);
              backgroundCircle.setAttribute('cy', center);
              backgroundCircle.setAttribute('r', RING_RADIUS);
              backgroundCircle.setAttribute('class', 'timer-ring-bg');

              const progressCircle = document.createElementNS(svgNS, 'circle');
              progressCircle.setAttribute('cx', center);
              progressCircle.setAttribute('cy', center);
              progressCircle.setAttribute('r', RING_RADIUS);
              progressCircle.setAttribute('class', 'timer-ring-progress');
              progressCircle.style.strokeDasharray = RING_CIRCUMFERENCE;

              const text = document.createElementNS(svgNS, 'text');
              text.setAttribute('x', '50%');
              text.setAttribute('y', '50%');
              text.setAttribute('dy', '.3em');
              text.setAttribute('class', 'timer-ring-display');

              svg.appendChild(backgroundCircle);
              svg.appendChild(progressCircle);
              svg.appendChild(text);
              this.elements.ringContainer.prepend(svg);
              this.ringProgress = progressCircle;
              this.ringText = text;
              log('TrainerComponent', 'Timer ring SVG created and references set.');
          }

          updateTimerRing(percent) {
              if (!this.ringProgress) return;
              const offset = RING_CIRCUMFERENCE - (percent / 100) * RING_CIRCUMFERENCE;
              this.ringProgress.style.strokeDashoffset = offset;
          }

          render(state) {
              const { exerciseQueue, currentExerciseIndex, status } = state;

              if (currentExerciseIndex < 0 || currentExerciseIndex >= exerciseQueue.length) {
                  this.elements.exerciseName.textContent = '';
                  this.elements.exerciseDetails.textContent = '';
                  this.elements.repDisplay.textContent = '';
                  this.elements.upcomingDisplay.innerHTML = '';
                  this.elements.btnStart.classList.add('hidden');
                  this.elements.btnPause.classList.add('hidden');
                  this.elements.btnResume.classList.add('hidden');
                  return;
              };

              const currentExercise = exerciseQueue[currentExerciseIndex];
              this.elements.exerciseName.textContent = currentExercise.name;
              this.elements.exerciseDetails.textContent = this.formatExerciseDetails(state);
              this.elements.repDisplay.textContent = (status === 'running') ? `${UI_TEXT.TRAINER_REP_LABEL} ${state.currentRep}` : '';

              const upcomingExercises = exerciseQueue.slice(currentExerciseIndex + 1, currentExerciseIndex + 3).map(ex => ex.name).join(', ');
              if (upcomingExercises) {
                this.elements.upcomingDisplay.innerHTML = `<strong>${UI_TEXT.TRAINER_UPCOMING_LABEL}</strong> ${upcomingExercises}`;
              } else {
                this.elements.upcomingDisplay.innerHTML = '';
              }

              this.elements.btnStart.classList.toggle('hidden', status !== 'idle');
              this.elements.btnPause.classList.toggle('hidden', status !== 'running' && status !== 'resting');
              this.elements.btnResume.classList.toggle('hidden', status !== 'paused');

              this.elements.modeTempoContainer.classList.remove('hidden');
              
              if (!this.ringText) {
                log('TrainerComponent', 'Render called but ringText is null. Aborting render of ring.');
                return;
              }

              if (status === 'running') this.renderPhase(state);
              else if (status === 'resting') this.renderRest(state);
              else if (status === 'paused') this.renderPaused(state);
              else if (status === 'idle') {
                  this.ringText.textContent = '';
                  this.ringText.classList.remove('flashing');
                  this.updateTimerRing(0);
              }
          }

          renderPhase(state) {
              const { executionQueue, currentPhaseIndex, phaseTimeElapsed } = state;
              const phase = executionQueue[currentPhaseIndex];
              if (!phase) return;

              const phaseNameDisplay = phase.name.replace('pre-', '').toUpperCase();
              const isPrePhase = phase.name.startsWith('pre-');

              this.ringText.textContent = phaseNameDisplay;
              this.ringText.classList.toggle('flashing', isPrePhase);

              if (isPrePhase) {
                  this.updateTimerRing(100);
              } else {
                  const progressPercent = Math.min(100, (phaseTimeElapsed / phase.duration) * 100);
                  this.updateTimerRing(progressPercent);
              }
          }

          renderRest(state) {
              const { restTimeRemaining } = state;
              this.ringText.textContent = UI_TEXT.TRAINER_REST_LABEL;
              this.ringText.classList.remove('flashing');
              const totalRest = state.exerciseQueue[state.currentExerciseIndex].defaultRest * 1000;
              const progressPercent = (totalRest > 0) ? ((totalRest - restTimeRemaining) / totalRest) * 100 : 100;
              this.updateTimerRing(progressPercent);
          }
          
          renderPaused() {
            this.ringText.textContent = UI_TEXT.TRAINER_PAUSED_LABEL;
            this.ringText.classList.remove('flashing');
          }

          formatExerciseDetails(state) {
              const { exerciseQueue, currentExerciseIndex, currentSet } = state;
              const exercise = exerciseQueue[currentExerciseIndex];
              if (!exercise) return '';
              const sets = exercise.defaultSets;
              let details = `Serie ${currentSet} di ${sets}`;
              if (exercise.type === 'reps') {
                  const reps = exercise.defaultReps;
                  details += ` | ${reps} Ripetizioni`;
              } else if (exercise.type === 'time') {
                  const duration = exercise.defaultDuration;
                  details += ` | ${duration}s`;
              }
              return details;
          }
      }
commands:
  - "echo 'Correzioni UI applicate: layout del Trainer ottimizzato e bug di wrapping nel modale risolto.'"